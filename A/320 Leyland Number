#include<iostream>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int pow(int a, int b)
{
    int x = 1;
    rep(i, b) x *= a;
    return x;
}

int main()
{
    int a, b;
    cin>>a>>b;
    cout<<pow(a,b)+pow(b,a)<<endl;
    return 0;
}
