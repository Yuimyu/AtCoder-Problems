#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main()
{
    string n;
    cin>>n;

    for(int i = 0; i+1<n.size(); i++)
    {
        if(n[i] <= n[i+1])
        {
            cout<<"No";
            return 0;
        }
    }

    cout<<"Yes";

    return 0;
}
