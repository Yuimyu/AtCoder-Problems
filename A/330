#include<iostream>

using namespace std;

int main()
{ 
   int n, l, a, x=0;

   cin>>n>>l;

   for(int i = 0; i < n; i++)
   {
        cin>>a;
        if(a>=l)
        {
            x++;
        }
   }

   cout<<x;
   
    return 0;
}
