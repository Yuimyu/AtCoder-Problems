#include<iostream>
#include<vector>

using namespace std;

int main()
{
   int n;
   cin>>n;
   vector<int> a(n);
   for(int i=0;i<n;i++) cin>>a[i];
   
    int test=a[0];
   for(int i = 1; i<n; i++) if(test!=a[i]){cout<<"No"; return 0;}
   cout<<"Yes";

    return 0;
}
