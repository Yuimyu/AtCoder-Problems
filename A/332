#include<iostream>

using namespace std;

int main()
{ 
   int n, s, k, p, q, sum=0;

   cin>>n>>s>>k;

   for(int i = 0; i<n; i++)
   {
        cin>>p>>q;
        sum+=p*q;
   }

    if(sum>= s)
    {
        cout<<sum;
    }

    else 
    {
        cout<<sum+k;
    }

    return 0;
}
